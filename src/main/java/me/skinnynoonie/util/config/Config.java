package me.skinnynoonie.util.config;

public interface Config {

    /**
     * Checks if this configuration is valid or not.
     *
     * @return {@code true} if the config is valid, otherwise {@code false}.
     */
    boolean isValid();

}
